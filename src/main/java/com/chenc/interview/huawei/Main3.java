package com.chenc.interview.huawei;
import java.util.*;

// We have imported the necessary tool classes.
// If you need to import additional packages or classes, please import here.


// We have imported the necessary tool classes.
// If you need to import additional packages or classes, please import here.

public class Main3 {
    public static void main(String[] args) {
    // please define the JAVA input here. For example: Scanner s = new Scanner(System.in);
    // please finish the function body here.
    // please define the JAVA output here. For example: System.out.println(s.nextInt());
        // Scanner sc = new Scanner(System.in);
        // String line1 = sc.nextLine();
        // line1 = line1.substring(1, line1.length()-1);
        // String[] numsS = line1.split(",");
        // int[] numsOri = new int[numsS.length];
        // for (int i = 0;i<numsOri.length;i++){
        //     numsOri[i] = Integer.parseInt(numsS[i]);
        // }
        // String[] line2 = sc.nextLine().split("/");
        // int[] nums2 = new int[line2.length];
        // for (int i = 1;i<line2.length;i++){
        //     nums2[i] = Integer.parseInt(line2[i]);
        // }
        // String line3 = sc.nextLine();
        // line3 = line3.substring(1, line3.length()-1);
        // String[] numsS2 = line3.split(",");
        // int[] numsTar = new int[numsS2.length];
        // for (int i = 0;i<numsTar.length;i++){
        //     numsTar[i] = Integer.parseInt(numsS2[i]);
        // }
        // System.out.print("[");
        // for (int i = 0;i<numsTar.length -1;i++){
        //     System.out.print(numsTar[i] +",");
        // }
        // System.out.println(numsTar[numsTar.length -1] + "]");

        // Queue<Integer> q = new LinkedList<>();
        // q.add(1);
        // q.peek();
        System.out.println((1 & 20) == 1);
    }

}
